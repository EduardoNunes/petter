import Logo from "@/components/Logo/Logo";

export default function Home() {
  return (
    <div className="flex items-center justify-center h-[100vh] w-[100vw] bg-gradient-to-r from-azulPalido to-branco">
      <Logo />
    </div>
  );
}
